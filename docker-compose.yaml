version: "3"

services:
  dapp-web:
    image: dapp-web
    build:
        context: .
        dockerfile: Dockerfile
    restart: always
    volumes:
      - ./:/webapps
    ports:
      - 5554:80
    networks:
      - base-network



networks:
  base-network:
    external: true